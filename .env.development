# ============================================================
# PortAI Nexus — 开发环境默认配置
# ============================================================
# 此文件提交到 Git，作为团队开发环境的共享默认值。
# 个人覆盖请使用 .env.local（已在 .gitignore 中）。
#
# 优先级（从高到低）：
#   1. 命令行参数（PORT=3001 pnpm dev）
#   2. .env.local（个人覆盖，不提交）
#   3. .env.development（本文件，团队共享）
#   4. config.ts 硬编码默认值
#
# 整改方案 v2.1 — B-05 配置管理分层
# ============================================================

# ── 应用基础 ──
NODE_ENV=development
PORT=3000
LOG_LEVEL=info
APP_NAME=PortAI Nexus
APP_VERSION=4.0.0

# ── MySQL（Docker Compose 默认值） ──
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=portai_nexus

# ── Redis（Docker Compose 默认值） ──
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ── ClickHouse（Docker Compose 默认值） ──
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=portai_nexus

# ── Kafka（Docker Compose 默认值） ──
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=portai-nexus
KAFKA_GROUP_ID=portai-nexus-group

# ── Neo4j（Docker Compose 默认值） ──
NEO4J_HOST=localhost
NEO4J_PORT=7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j

# ── Qdrant（Docker Compose 默认值） ──
QDRANT_HOST=localhost
QDRANT_PORT=6333

# ── MinIO（Docker Compose 默认值） ──
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=portai-nexus

# ── Elasticsearch（Docker Compose 默认值） ──
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# ── InfluxDB（Docker Compose 默认值） ──
INFLUXDB_HOST=localhost
INFLUXDB_PORT=8086

# ── MQTT（Docker Compose 默认值） ──
MQTT_HOST=localhost
MQTT_PORT=1883

# ── OTel 可观测性 ──
OTEL_ENABLED=true
OTEL_SERVICE_NAME=portai-nexus-dev
OTEL_SAMPLING_RATIO=1.0

# ── 安全（开发环境宽松配置） ──
JWT_SECRET=dev-only-secret-change-in-production
CORS_ORIGINS=*
