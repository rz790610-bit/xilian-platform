# 基础数据模块设计方案

根据您提供的数据库设计文档，规划以下基础数据模块：

## 模块结构

### Layer 1: 系统配置层（6个子模块）

| 序号 | 模块名称 | 路由 | 功能描述 |
|------|---------|------|----------|
| 1 | 编码规则 | /base/coding-rules | 4级自动编码生成规则配置 |
| 2 | 部门编码 | /base/departments | 5级部门编码体系管理 |
| 3 | 设备类别 | /base/device-categories | 20个设备分类管理 |
| 4 | 计量单位 | /base/units | 计量单位配置 |
| 5 | 诊断标准 | /base/diagnosis-standards | 诊断标准配置 |
| 6 | 阈值标准 | /base/threshold-standards | 告警阈值配置 |

## 各模块详细设计

### 1. 编码规则模块

**功能：** 支持4级自动编码生成

**数据结构：**
```typescript
interface CodingRule {
  id: string;
  level: 1 | 2 | 3 | 4;        // 编码级别
  code: string;                 // 编码值，如 M, gj, XC, 001
  name: string;                 // 名称
  description?: string;         // 描述
  parentId?: string;            // 父级ID
  autoIncrement: boolean;       // 是否自动递增
  currentSeq: number;           // 当前序号
}
```

**示例：** Mgj-XC002
- 第1级: M (类型)
- 第2级: gj (规格)
- 第3级: XC (系列)
- 第4级: 002 (序号，自动生成)

### 2. 部门编码模块

**功能：** 5级部门编码体系

**编码规则：** 633A011104
- 第1级(3位): 633 = 地区（日照市）
- 第2级(1位): A = 行业（港口）
- 第3级(2位): 01 = 企业（日照港集团）
- 第4级(2位): 11 = 分公司（岚山）
- 第5级(2位): 04 = 部门队（矿石输运）

**数据结构：**
```typescript
interface Department {
  id: string;
  code: string;                 // 完整编码
  level: 1 | 2 | 3 | 4 | 5;    // 层级
  name: string;                 // 名称
  parentId?: string;            // 父级ID
  regionCode?: string;          // 地区编码
  industryCode?: string;        // 行业编码
  enterpriseCode?: string;      // 企业编码
  branchCode?: string;          // 分公司编码
  deptCode?: string;            // 部门编码
}
```

### 3. 设备类别模块

**功能：** 20个设备分类管理

**预置分类：**
1. 输送机械
2. 起重机械
3. 泵类设备
4. 风机设备
5. 压缩机
6. 电机设备
7. 减速机
8. 液压设备
9. 气动设备
10. 阀门管道
11. 换热设备
12. 反应设备
13. 储罐容器
14. 分离设备
15. 干燥设备
16. 破碎设备
17. 筛分设备
18. 给料设备
19. 包装设备
20. 其他设备

### 4. 计量单位模块

**功能：** 计量单位配置

**预置单位类别：**
- 长度：mm, cm, m, km
- 质量：g, kg, t
- 时间：s, min, h
- 温度：℃, K
- 压力：Pa, kPa, MPa, bar
- 流量：m³/h, L/min
- 速度：m/s, mm/s, rpm
- 振动：mm/s, g, μm

### 5. 诊断标准模块

**功能：** 诊断标准配置

**数据结构：**
```typescript
interface DiagnosisStandard {
  id: string;
  code: string;                 // 标准编号
  name: string;                 // 标准名称
  category: string;             // 分类（国标/行标/企标）
  equipmentType: string;        // 适用设备类型
  parameters: string[];         // 诊断参数
  description: string;          // 标准描述
  version: string;              // 版本
  effectiveDate: string;        // 生效日期
}
```

### 6. 阈值标准模块

**功能：** 告警阈值配置

**数据结构：**
```typescript
interface ThresholdStandard {
  id: string;
  name: string;                 // 名称
  equipmentType: string;        // 设备类型
  parameter: string;            // 参数名称
  unit: string;                 // 单位
  normalMin: number;            // 正常下限
  normalMax: number;            // 正常上限
  warningMin: number;           // 预警下限
  warningMax: number;           // 预警上限
  alarmMin: number;             // 报警下限
  alarmMax: number;             // 报警上限
  dangerMin: number;            // 危险下限
  dangerMax: number;            // 危险上限
}
```

## 界面设计

### 导航结构
```
📦 基础数据
  ├── 📋 编码规则
  ├── 🏢 部门编码
  ├── 🔧 设备类别
  ├── 📏 计量单位
  ├── 📊 诊断标准
  └── ⚠️ 阈值标准
```

### 通用功能
- 数据表格展示（支持分页、排序、筛选）
- 新增/编辑/删除操作
- 批量导入（Excel/CSV）
- 数据导出
- 树形结构展示（编码规则、部门编码）

## 确认事项

请确认以下内容：
1. 模块划分是否合理？
2. 数据结构是否满足需求？
3. 是否需要增加其他子模块？
4. 界面风格是否与现有系统保持一致？

---
确认后我将开始实现。
