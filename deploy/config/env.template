# ============================================================
# 西联智能平台 - 环境变量配置模板
# XiLian Intelligent Platform - Environment Configuration
# ============================================================
# 
# 使用说明：
# 1. 复制此文件为 .env: cp env.template .env
# 2. 根据实际情况修改配置值
# 3. 生产环境务必修改所有密码和密钥
# ============================================================

# ==================== 应用配置 ====================

# 应用端口
APP_PORT=3000

# 运行环境: development | production
NODE_ENV=production

# JWT 密钥 (生产环境必须修改!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ==================== MySQL 数据库 ====================

# MySQL 端口
MYSQL_PORT=3306

# MySQL Root 密码 (生产环境必须修改!)
MYSQL_ROOT_PASSWORD=root123

# 数据库名称
MYSQL_DATABASE=xilian

# 数据库用户名
MYSQL_USER=xilian

# 数据库密码 (生产环境必须修改!)
MYSQL_PASSWORD=xilian123

# ==================== Redis 缓存 ====================

# Redis 端口
REDIS_PORT=6379

# Redis 密码 (可选，默认无密码)
# REDIS_PASSWORD=

# ==================== ClickHouse 时序数据库 ====================

# ClickHouse HTTP 端口
CLICKHOUSE_HTTP_PORT=8123

# ClickHouse TCP 端口
CLICKHOUSE_TCP_PORT=9000

# ClickHouse 数据库名
CLICKHOUSE_DB=xilian

# ClickHouse 用户名
CLICKHOUSE_USER=default

# ClickHouse 密码 (可选)
CLICKHOUSE_PASSWORD=

# ==================== Kafka 消息队列 ====================

# Kafka 内部端口
KAFKA_PORT=9092

# Kafka 外部访问端口
KAFKA_EXTERNAL_PORT=29092

# Zookeeper 端口
ZOOKEEPER_PORT=2181

# ==================== 监控服务 ====================

# Prometheus 端口
PROMETHEUS_PORT=9090

# Grafana 端口
GRAFANA_PORT=3001

# Grafana 管理员用户名
GRAFANA_USER=admin

# Grafana 管理员密码 (生产环境必须修改!)
GRAFANA_PASSWORD=admin123

# ==================== 可选服务 ====================

# MinIO 对象存储
MINIO_API_PORT=9001
MINIO_CONSOLE_PORT=9002
MINIO_USER=minioadmin
MINIO_PASSWORD=minioadmin123

# Nginx 反向代理
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ==================== 外部服务集成 ====================

# OpenAI API (可选，用于 AI 功能)
# OPENAI_API_KEY=sk-xxx

# 阿里云 OSS (可选，用于文件存储)
# ALIYUN_OSS_ACCESS_KEY=
# ALIYUN_OSS_SECRET_KEY=
# ALIYUN_OSS_BUCKET=
# ALIYUN_OSS_REGION=

# 邮件服务 (可选，用于通知)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@example.com

# ==================== 安全配置 ====================

# 允许的 CORS 来源 (多个用逗号分隔)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Session 过期时间 (秒)
SESSION_EXPIRE=86400

# 请求速率限制 (每分钟)
RATE_LIMIT=100
